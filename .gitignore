paper/
__pycache__/
*.pyc
.venv/
.venv-*/
cufile.log
06_simulated_genome_scan/data/

# Scratch directories the moments-feature scripts write during a run
# (replicate VCFs, recombination maps, samples files, pickled moments
# LD stats). These are caches the scripts re-fill on demand, not paper
# artifacts.
01_accuracy/tables/four_pop_ld_demo_data/
01_accuracy/tables/moments_3pop_integration_demo/
01_accuracy/tables/moments_ld_decay_comparison_data/
01_accuracy/tables/multipop_ld_benchmark_cache/

# Local agent / IDE state
.claude/
.vscode/
.idea/
.DS_Store
CLAUDE.md

# TeX build artifacts (if a build ever lands inside the repo)
*.aux
*.bbl
*.bcf
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.toc
